/**
 * Constant for Carriage Return + Line Feed (CRLF) used in SMTP communication.
 */
export const CRLF = "\r\n";

/**
 * Default hostname for the SMTP server.
 */
export const DEFAULT_HOSTNAME = "127.0.0.1";

/**
 * Default port for the SMTP server.
 */
export const DEFAULT_PORT = 2525;

/**
 * Default domain name for the SMTP server.
 */
export const DEFAULT_DOMAIN = "smtpsaurus.email";
